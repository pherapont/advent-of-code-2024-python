def check_calibration(nums: list[int], res: int) -> bool:
    expressions = [nums[0]]
    operators = ("+", "*")
    for n in nums[1:]:
        ...
    # TODO разработать перестановки чисел и операторов. Затем eval()
